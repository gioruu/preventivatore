{
  "name": "preventivatore",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npx serve . --listen 3000",
    "test": "node --test",
    "deploy": "npx wrangler pages deploy . --project-name=preventivatore"
  }
}
